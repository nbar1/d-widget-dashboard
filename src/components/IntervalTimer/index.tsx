import styled from 'styled-components';

const IntervalTimerContainer = styled.div`
  background-color: #fff;
  box-shadow: #e7e7e7 0 0 2px 0;
  border-radius: 15px;
  min-height: 496px;
  width: 380px;
`;

const IntervalTimer = () => {
  return <IntervalTimerContainer>Interval Timer</IntervalTimerContainer>;
};

export default IntervalTimer;
